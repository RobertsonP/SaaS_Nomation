# Database Configuration (use 'postgres' for Docker, 'localhost' for local development)
DATABASE_URL="postgresql://nomation_user:nomation_password@postgres:5432/nomation"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# API Configuration
PORT=3002
NODE_ENV=development

# Frontend Configuration
VITE_API_URL=http://localhost:3002

# Playwright Configuration (for Docker)
PLAYWRIGHT_BROWSERS_PATH=/opt/playwright
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=false

# Docker Database Configuration (used by docker-compose)
POSTGRES_DB=nomation
POSTGRES_USER=nomation_user
POSTGRES_PASSWORD=nomation_password